import { useQuery } from "@tanstack/react-query"; import { EmptyState } from "@/components/app/EmptyState"; import { getActiveOrganization, loadDashboardData } from "@/lib/beauty/repository";
export default function CustomersPage(){const org=useQuery({queryKey:["org"],queryFn:getActiveOrganization}); const data=useQuery({queryKey:["customers",org.data?.id],enabled:!!org.data?.id,queryFn:()=>loadDashboardData(org.data.id)}); const customers=data.data?.customers??[]; return <div className="space-y-6"><h2 className="text-3xl font-black">Clientes</h2>{customers.length?<div className="overflow-hidden rounded-3xl bg-white"><table className="w-full text-sm"><thead className="bg-stone-100 text-left"><tr><th className="p-4">Nome</th><th>Telefone</th><th>Status</th><th>Faltas</th><th>Total gasto</th></tr></thead><tbody>{customers.map((c:any)=><tr className="border-t" key={c.id}><td className="p-4 font-medium">{c.full_name}</td><td>{c.phone_e164}</td><td>{c.status}</td><td>{c.no_show_count}</td><td>{Number(c.estimated_lifetime_value).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})}</td></tr>)}</tbody></table></div>:<EmptyState title="Nenhum cliente cadastrado" description="Clientes serão criados por mensagens recebidas, cadastro manual ou agendamentos."/>}</div>}
